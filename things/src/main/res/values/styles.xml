<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar" />

    <style name="VikiTheme" parent="AppTheme" >
        <item name="android:colorPrimary">@android:color/holo_red_light</item>
        <item name="android:colorPrimaryDark">@android:color/holo_red_light</item>
        <item name="android:colorAccent">?android:attr/colorMultiSelectHighlight</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="android:navigationBarColor">@color/primary_material_dark</item>
    </style>

</resources>
